<Window x:Class="K_NECT.Views.Estudiante.MisAsignaturasView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="K-NECT - Mis Asignaturas" 
        Height="700" 
        Width="1200"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        MouseDown="Window_MouseDown"
        Icon="/Resources/Images/LogoK-nect3.png">

    <Border Background="{StaticResource White}" 
            CornerRadius="15" 
            Margin="20"
            BorderBrush="{StaticResource GrayLight}"
            BorderThickness="1">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ===== HEADER ===== -->
            <Border Grid.Row="0" 
                    Background="{StaticResource PrimaryMedium}" 
                    CornerRadius="15,15,0,0"
                    Padding="25,15">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Botón Volver -->
                    <Button Grid.Column="0"
                            Content="← Volver"
                            Background="Transparent"
                            Foreground="{StaticResource White}"
                            BorderThickness="0"
                            FontSize="14"
                            Cursor="Hand"
                            Click="BtnVolver_Click"/>

                    <!-- Título -->
                    <TextBlock Grid.Column="1"
                               Text="📚 MIS ASIGNATURAS"
                               Foreground="{StaticResource White}"
                               FontSize="20"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>

                    <!-- Botón Cerrar -->
                    <Button Grid.Column="2"
                            Content="×"
                            Background="Transparent"
                            Foreground="{StaticResource White}"
                            BorderThickness="0"
                            FontSize="24"
                            FontWeight="Bold"
                            Cursor="Hand"
                            Click="BtnCerrar_Click"/>
                </Grid>
            </Border>

            <!-- ===== INFORMACIÓN DEL ESTUDIANTE ===== -->
            <Border Grid.Row="1" 
                    Background="{StaticResource PrimaryLight}"
                    Padding="25,20">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Info del estudiante -->
                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="txtEstudiante" 
                                  Text="Estudiante: Nombre Completo"
                                  Foreground="{StaticResource White}"
                                  FontSize="16"
                                  FontWeight="Bold"/>
                        <TextBlock x:Name="txtCarreraSemestre"
                                  Text="Carrera - Semestre"
                                  Foreground="{StaticResource GrayLight}"
                                  FontSize="13"
                                  Margin="0,5,0,0"/>
                    </StackPanel>

                    <!-- Estadísticas -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Border Background="{StaticResource PrimaryDark}"
                                CornerRadius="8"
                                Padding="15,10"
                                Margin="0,0,10,0">
                            <StackPanel>
                                <TextBlock Text="ASIGNATURAS"
                                          Foreground="{StaticResource GrayLight}"
                                          FontSize="10"
                                          HorizontalAlignment="Center"/>
                                <TextBlock x:Name="txtCantidadAsignaturas"
                                          Text="0"
                                          Foreground="{StaticResource White}"
                                          FontSize="20"
                                          FontWeight="Bold"
                                          HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <Border Background="{StaticResource SuccessDark}"
                                CornerRadius="8"
                                Padding="15,10">
                            <StackPanel>
                                <TextBlock Text="PROMEDIO GENERAL"
                                          Foreground="{StaticResource White}"
                                          FontSize="10"
                                          HorizontalAlignment="Center"/>
                                <TextBlock x:Name="txtPromedioGeneral"
                                          Text="0.0"
                                          Foreground="{StaticResource White}"
                                          FontSize="20"
                                          FontWeight="Bold"
                                          HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ===== LISTA DE ASIGNATURAS ===== -->
            <ScrollViewer Grid.Row="2" 
                         VerticalScrollBarVisibility="Auto"
                         Margin="25,20,25,25">

                <StackPanel x:Name="pnlAsignaturas">
                    <!-- Las tarjetas de asignaturas se agregarán aquí dinámicamente -->
                </StackPanel>
            </ScrollViewer>

            <!-- Mensaje cuando no hay asignaturas -->
            <StackPanel Grid.Row="2"
                       x:Name="pnlSinAsignaturas"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       Visibility="Collapsed">
                <TextBlock Text="📚"
                          FontSize="48"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,15"/>
                <TextBlock Text="No tienes asignaturas inscritas aún"
                          Foreground="{StaticResource TextLight}"
                          FontSize="16"
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>